import 'package:flutter/cupertino.dart';

class SampleDefaultPage extends StatelessWidget {
  const SampleDefaultPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sample Default Page'),
    );
  }
}
